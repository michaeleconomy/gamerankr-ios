import UIKit
class PlaceholderImages {
    static public let game = UIImage(named: "picture.png")
    static public let user = UIImage(named: "person.png")
    static public let loadingBar = UIImage.animatedImageNamed("loading-bar-", duration: 1.0)
}
