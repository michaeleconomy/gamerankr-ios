query UserRankings($id: ID!, $after: String) {
    user(id: $id) {
        rankings(first: 30, after: $after) {
            edges {
                ranking : node {
                    ...RankingBasic
                }
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}
