query Following($userId: ID!, $after: String) {
    user(id: $userId) {
        following(first: 30, after: $after) {
            edges {
                user : node {
                    ...UserBasic
                }
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}
