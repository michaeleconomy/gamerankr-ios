query Game($id: ID!) {
    game(id: $id) {
        ...GameBasic
        ports {
            id
            description
        }
        rankings(first: 30) {
            edges {
                node {
                    ...RankingWithUser
                }
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
        friend_rankings {
            ...RankingWithUser
        }
    }
}
