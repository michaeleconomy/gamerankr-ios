query Game($id: ID!) {
    game(id: $id) {
        ...GameBasic
        ports {
            medium_image_url
        }
        rankings {
            id
            ranking
            review
            user {
                id
                real_name
                photo_url
            }
        }
    }
}
