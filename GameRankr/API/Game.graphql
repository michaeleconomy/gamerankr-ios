query Game($id: ID!) {
    game(id: $id) {
        ...GameBasic
        ports {
            large_image_url
        }
        rankings {
            id
            ranking
            review
            user {
                id
                real_name
                photo_url
            }
        }
    }
}
